using UnityEngine;

public class Temp : MonoBehaviour
{
    //[SerializeField] private Transform a;
    //[SerializeField] private Transform b;
    //[SerializeField] private Transform c;

    //[SerializeField] private LineRenderer _lineRenderer;

    // Update is called once per frame
    private void Update()
    {
        //Vector3 local = transform.InverseTransformPoint(0, 5, 0);
        //transform.localPosition = local;
        //Vector3 direction = (a.position + b.position) / 2;
        //// Calculate the rotation to align the direction with the forward axis (0, 0, 1)
        //c.rotation = Quaternion.LookRotation(direction, direction);
        //Vector3[] positions = { a.transform.position, b.transform.position, c.transform.position };
        //_lineRenderer.SetPositions(positions);
    }
}
